namespace Turbo.Primitives.Rooms.Enums.Wired;

/// <summary>How "move user to furni" brings the avatar there (user_move.walkmode.0..2).</summary>
public enum WiredUserWalkModeType
{
    Walk = 0,
    Slide = 1,
    Teleport = 2,
}
