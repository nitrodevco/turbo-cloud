namespace Turbo.Primitives.Rooms.Enums.Wired;

/// <summary>
/// What a variable fx draws, as the client numbers its categories (the <c>variablefx</c>
/// category registry). There is one wired addon per category.
/// </summary>
public enum VariableFxCategoryType
{
    HealthPoints = 0,
    ProgressBar = 1,
    LevellingProgress = 2,
    StatusBar = 3,
    BossBar = 4,
    NumberDisplay = 5,
}
