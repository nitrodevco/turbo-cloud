namespace Turbo.Primitives.Rooms.Enums.Wired;

/// <summary>When the client draws a variable fx it holds a status for. It applies this itself.</summary>
public enum VariableFxShowModeType
{
    Always = 0,
    WhenVariableChanges = 1,
    Never = 2,
}
