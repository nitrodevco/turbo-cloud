namespace Turbo.Primitives.Pets.Enums;

/// <summary>What a pet supply item (bowl, bottle, toy, basket) gives a pet that reaches it.</summary>
public enum PetSupplyType
{
    Food,
    Drink,
    Toy,
    Nest,
}
